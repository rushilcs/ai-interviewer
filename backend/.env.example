# backend/.env.example
# Postgres
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/ai_interviewer

# Auth (ops)
JWT_SECRET=change-me-in-real-env

# Seed script: ops admin user (used by npm run seed)
OPS_ADMIN_EMAIL=ops-admin@example.com
OPS_ADMIN_PASSWORD=ops-admin-password

# Base URL for invite links (used in POST /api/interview-invites response)
INVITE_BASE_URL=http://localhost:3000

# Assistant (OpenAI)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Invite tokens (optional; only needed if you store token hashes instead of raw tokens)
INVITE_TOKEN_SALT=change-me-if-using-hash
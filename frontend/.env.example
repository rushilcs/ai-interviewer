# Optional: override backend API URL (defaults to http://localhost:4000).
# Required in production (e.g. Vercel): set to your Render backend URL.
# NEXT_PUBLIC_BACKEND_URL=https://your-backend.onrender.com
